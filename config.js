// Single place to wire the forms to a real backend.
// Set FEEDBACK_ENDPOINT to a Formspree form URL (e.g. 'https://formspree.io/f/xxxxxxx')
// or any endpoint accepting a JSON POST. All forms on every page use this.
window.RECHARGE_CONFIG = {
  FEEDBACK_ENDPOINT: '',
};
